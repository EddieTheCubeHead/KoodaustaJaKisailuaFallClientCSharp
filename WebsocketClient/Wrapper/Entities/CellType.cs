namespace WebsocketClient.Wrapper.Entities;

/// <summary>
/// An enum containing all different types a map shell can be
/// </summary>
public enum CellType
{
    Empty,
    OutOfVision,
    AudioSignature,
    HitBox,
    Ship,
    Projectile
}